.section {
    width: 100%; height: 100vh; display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center; justify-content: center; position: relative;
    padding: 10vh 5vw;
    overflow: hidden; /* THÊM DÒNG NÀY ĐỂ KHÔNG BAO GIỜ BỊ LEM */
  }
